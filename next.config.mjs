/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/booking-bubbles',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
